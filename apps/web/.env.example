# Set to point the web app at apps/api. Unset = replay mock/events.json.
# VITE_API_URL=http://localhost:8000
# Public origin used for link-preview (og:image) URLs; set to the deployed domain.
VITE_SITE_URL=http://localhost:5173
