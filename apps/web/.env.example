# Where apps/api runs. Leave EMPTY (VITE_API_URL=) to use the recorded mock with no backend.
VITE_API_URL=http://localhost:8000
# Public origin for link-preview (og:image) URLs; set to the deployed domain.
VITE_SITE_URL=http://localhost:5173
