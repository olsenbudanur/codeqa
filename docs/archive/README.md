# Do not read

Superseded drafts kept for history only. Do not use them as specs.

- `system_design.md` — first system design (nine-stream split). Superseded by `docs/components.md`; its data sources table moved to `docs/data_sources.md`; its contracts moved to `docs/contracts.md`.
- `workstreams.md` — nine-stream work split with stream letters A–I. Superseded by `docs/components.md` (final component names) and `docs/gap_specs.md` (parallel vs sync). Its contracts C1–C10 moved to `docs/contracts.md`.

The two published artifact pages (system design, workstreams) mirror these drafts and are likewise historical.
